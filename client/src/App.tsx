/**
 * STYLE REMINDER — Literal SmartStaker mirror: the reference is the page source.
 * Do not introduce a second application shell or alter any reference wording.
 */
import ErrorBoundary from "@/components/ErrorBoundary";
import { LiteralMirror } from "@/components/LiteralMirror";

export default function App() {
  return <ErrorBoundary><LiteralMirror /></ErrorBoundary>;
}
